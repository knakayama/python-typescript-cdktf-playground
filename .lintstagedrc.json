{
  "*": ["prettier --write --ignore-unknown", "ec", "gitleaks detect -v"],
  "{src,scripts}/**/*.{ts,mts}": "eslint --fix",
  "app/**/*.py": [
    "poetry run -C app ruff --fix",
    "poetry run -C app black",
    "poetry run -C app mypy"
  ]
}
